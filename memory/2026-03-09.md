# 2026-03-09 — Sesión de trabajo

## Proyecto activo
- **Hackathon Bitcoin México 2026 (P-17)**
- Proyecto: **El Tesoro del Pueblo**
- URL producción: https://el-tesoro-del-pueblo.vercel.app
- Archivo principal: `el-tesoro-del-pueblo/index.html`
- Assets locales: `bgm.mp3`, `sat-icon.png`, `basket.png`

## Lo que se hizo hoy

### Música de fondo
- Añadido `<audio id="bgm-audio" loop>` con `bgm.mp3` (descargado de Google Drive del usuario)
- Botón 🎵/🔇 esquina superior derecha, solo visible durante diálogos (showHUD/hideHUD)
- Volumen: 8%
- Se reinicia al volver a jugar (`restartGame()` hace pause + currentTime=0)
- BGM inicia en `startGame()` al pulsar "Comenzar"

### Ícono Bitcoin kawaii
- Imagen: `sat-icon.png` — moneda Bitcoin kawaii (original RGBA 2816x1536 con transparencia)
- Constante `SAT_IMG` en JS para reutilizar el tag `<img>`
- Reemplaza ⚡ en toda la UI: HUD, contadores mini-juegos, mensajes resultado, feedback choices, títulos, botón claim, etc.
- Tamaño: `.sat-img { width: 4em; height: 4em }` — doble del original
- Items que caen en catch game: `width:4.8rem; height:auto` (height:auto evita padding transparente)

### Catch game (Acto I) — mejoras
- Canasta reemplazada: emoji 🎸 → imagen `basket.png` (sombrero custom del usuario, RGBA 740x283)
- Colisión pixel-perfect con `getBoundingClientRect()` — items desaparecen al tocar el sombrero
- Ajuste: `brimY = bRect.top + bRect.height * 0.70` para ignorar transparencia superior de la imagen
- `height:auto` en items de moneda para que bRect sea exacto al tamaño visible
- Subtítulo actualizado: "MUEVE EL SOMBRERO · ATRAPA [coin] · ESQUIVA 💸"

### UI / Visual
- Portrait personajes: `bottom: 220px → 255px`, `200px → 240px` (más arriba y más grande)
- Color El Cacique: `#4a1c00 → #cc2200` (rojo intenso visible)
- Logros desbloqueados muestran foto circular del personaje (vs emoji si bloqueado)
  - compadre → Pedro Infante
  - balero → El Cacique
  - ladona → María Félix
  - lightning → Cantinflas
  - hodler → Jorge Negrete
  - fronteras → Dolores del Río

### Sonidos
- `soundWin` y `soundLose`: volumen 28% → 15%

### Botón compartir
- Desktop: siempre abre X directamente
- Móvil: usa `navigator.share` nativo (mejor UX en touch)
- Fix: en Vercel (HTTPS) `navigator.share` estaba disponible en desktop y abría menú del SO

### Commits del día
- `94c3c5d` — música de fondo tipo vals mexicano (Web Audio API) — **reemplazado luego**
- `08746ed` — BGM bolero lento — **reemplazado luego**
- `9b32714` — botón música solo visible en diálogos
- `3089f67` — BGM desde Google Drive → luego reemplazado por local
- `630a49c` — bgm.mp3 servida localmente (evita CORS)
- `6af9fd0` — botón música esquina superior derecha
- `ab9bac0` — volumen BGM 8%
- `5d09cb6` — soundWin/soundLose 15%
- `343ce6c` — sat-icon.png original RGBA
- `e859ee5` — portrait más arriba y grande
- `889e9d2` — color El Cacique rojo
- `5b6beb6` — ícono Bitcoin kawaii en toda la UI
- `c2c2dde` — ícono doble de grande
- `eab608c` — sat-icon original con transparencia
- `09cf49c` — basket.png en catch game
- `5002fb8` — canasta 3.5rem
- `4fbe46b` — colisión catch game getBoundingClientRect
- `c05eb8f` — brimY 70% ajuste colisión
- `04342ef` — height:auto en monedas catch game
- `12ef623` — shareResult desktop → X directo
- `c91d3ad` — logros con fotos de personajes
- `623f538` — ⚡ → moneda Bitcoin en toda la UI

## Reglas importantes
- **NO deployar a Vercel automáticamente** — solo cuando el usuario lo indique
- Deploy command: `cd el-tesoro-del-pueblo && vercel --prod`
- Preview tunnel: reiniciar con `npx serve -l 3444` + `cloudflared tunnel --url http://localhost:3444`

## Pendiente
- Integración ZEBEDEE real (claim sats es mock)
- El texto del catch subtitle dice "MUEVE EL SOMBRERO" pero la imagen es del usuario (no es sombrero necesariamente — verificar con usuario)
